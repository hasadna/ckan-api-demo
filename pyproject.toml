[project]
name = "ckan-api-demo"
version = "0.1.0"
description = "Demo of CKAN API usage"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]
